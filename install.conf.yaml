- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.zshrc: zsh/.zshrc
    ~/.gitconfig: git/.gitconfig
    ~/.config/starship.toml: starship/starship.toml
    ~/.config/nvim: nvim/simple-nvim
